# Sample comment for all Queries

type Query {
# comment for findAllBooks query
  findAllBooks(pageNumber: Int , pageSize: Int): [Book]!
  findBookByUuid(uuid: String!) : Book
  countBooks: Long!

  findAllAuthors(pageNumber: Int,pageSize : Int) : [Author]!
  findAuthorByUuid(uuid:String!) : Author
  countAuthors: Long!
}
